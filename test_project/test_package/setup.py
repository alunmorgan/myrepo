from setuptools import setup, find_packages

setup(
      name="test_package",
      author = "Alun Morgan",
      version = "0.1",
      packages = find_packages(),
      install_requires = ['numpy'],
      )
#